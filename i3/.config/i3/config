# set modifier
set $super Mod4
set $alt Mod1

# set font
font pango: Noto Sans 10

# Use Mouse+$super to drag floating windows to their wanted position
floating_modifier $super

#autostart
exec_always --no-startup-id flashfocus
exec --no-startup-id xrandr --output DP-1 --scale 1.2 --right-of eDP-1
exec --no-startup-id nitrogen --head=0 --set-scaled --save ~/pictures/wallpapers/wallpaper.jpg
exec --no-startup-id nitrogen --head=1 --set-scaled --save ~/pictures/wallpapers/wallpaper.jpg
exec --no-startup-id libinput-gestures > /dev/null 2>&1 &
exec --no-startup-id xsettingsd &
exec_always --no-startup-id picom &
exec xinput set-prop "ELAN2602:00 04F3:3109 Touchpad"  "libinput Tapping Enabled" 1
exec setxkbmap -layout us,ru
exec setxkbmap -option 'grp:alt_shift_toggle'

# start a terminal
bindsym $super+Return exec alacritty

# start dmenu (a program launcher)
bindsym $super+d exec --no-startup-id rofi -show

# common apps keybinds
bindsym $super+l exec i3lock -i ~/pictures/wallpapers/wallpaper10.jpg
bindsym $super+Shift+w exec firefox
bindsym $mod+Print exec --no-startup-id maim --select "/home/moeryomenko/pictures/screenshots/$(date).png"

#change volume
bindsym XF86AudioRaiseVolume exec amixer -q set Master 5%+
bindsym XF86AudioLowerVolume exec amixer -q set Master 5%-
bindsym XF86AudioMute exec amixer set Master toggle

# music control
bindsym XF86AudioNext exec mpc next
bindsym XF86AudioPrev exec mpc prev
bindsym XF86AudioPlay exec mpc toggle
bindsym XF86AudioStop exec mpc stop

# kill focused window
bindsym $super+Shift+q kill

# change focus
bindsym $super+Left focus left
bindsym $super+Down focus down
bindsym $super+Up focus up
bindsym $super+Right focus right

# move focused window
bindsym $super+Shift+Left move left
bindsym $super+Shift+Down move down
bindsym $super+Shift+Up move up
bindsym $super+Shift+Right move right

# split in horizontal orientation
bindsym $super+h split h

# split in vertical orientation
bindsym $super+v split v

# enter fullscreen mode for the focused container
bindsym $super+f fullscreen toggle

# change container layout split
bindsym $super+s layout toggle split

# toggle tiling / floating
bindsym $super+space floating toggle

# change focus between tiling / floating windows
bindsym $super+Shift+space focus mode_toggle

# switch to workspace
bindsym $alt+$super+k workspace next
bindsym $alt+$super+j workspace prev
bindsym $super+1 workspace 1
bindsym $super+2 workspace 2
bindsym $super+3 workspace 3
bindsym $super+4 workspace 4
bindsym $super+5 workspace 5
bindsym $super+6 workspace 6

# move focused container to workspace
bindsym $super+Shift+1 move container to workspace 1
bindsym $super+Shift+2 move container to workspace 2
bindsym $super+Shift+3 move container to workspace 3
bindsym $super+Shift+4 move container to workspace 4
bindsym $super+Shift+5 move container to workspace 5
bindsym $super+Shift+6 move container to workspace 6

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $super+Shift+r restart

#
# Display brightness
#
bindsym XF86MonBrightnessDown exec brightnessctl s $(($(brightnessctl g) - 25))
bindsym XF86MonBrightnessUp exec brightnessctl s $(($(brightnessctl g) + 25))

# exit i3
bindsym $super+q exec "i3-nagbar -t warning -m 'Really, exit?' -b 'Yes' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        bindsym Left resize shrink width 5 px or 5 ppt
        bindsym Down resize grow height 5 px or 5 ppt
        bindsym Up resize shrink height 5 px or 5 ppt
        bindsym Right resize grow width 5 px or 5 ppt
        bindsym Return mode "default"
}
bindsym $super+r mode "resize"

# panel
bar {
    colors {
        background #2f343f
        statusline #2f343f
        separator #4b5262

        # colour of border, background, and text
        focused_workspace       #2f343f #bf616a #d8dee8
        active_workspace        #2f343f #2f343f #d8dee8
        inactive_workspace      #2f343f #2f343f #d8dee8
        urgent_workspacei       #2f343f #ebcb8b #2f343f
    }
    status_command i3status
    position top
}

# colour of border, background, text, indicator, and child_border
client.focused              #bf616a #2f343f #d8dee8 #bf616a #d8dee8
client.focused_inactive     #2f343f #2f343f #d8dee8 #2f343f #2f343f
client.unfocused            #2f343f #2f343f #d8dee8 #2f343f #2f343f
client.urgent               #2f343f #2f343f #d8dee8 #2f343f #2f343f
client.placeholder          #2f343f #2f343f #d8dee8 #2f343f #2f343f
client.background           #2f343f

for_window [class="^.*"] border pixel 0
gaps inner 10
