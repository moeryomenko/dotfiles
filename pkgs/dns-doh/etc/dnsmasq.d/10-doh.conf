# Forward every query to the local DoH proxy (dnsproxy on 127.0.0.1:5053).
# No plaintext upstreams — /etc/resolv.conf is no longer a source.
# Deploy: sudo install -Dm644 10-doh.conf /etc/dnsmasq.d/10-doh.conf
no-resolv
server=127.0.0.1#5053
