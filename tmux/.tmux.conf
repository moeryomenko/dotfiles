# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'joshmedeski/tmux-nerd-font-window-name'

set -g default-terminal "tmux-256color"
set -sg terminal-overrides ",*:RGB"
set -g display-time 0
set -g default-shell $SHELL

setw -g mouse off
set -s escape-time 0

set -g renumber-windows on # Renumber windows sequentially after closing any of them
set -g base-index 1        # Start windows and panes at 1, not 0
setw -g pane-base-index 1  # Set the base index for panes to 1 instead of 0
set -g history-limit 50000 # increase scrollback lines

set-option -g allow-rename off

# disable activity alerts
setw -g monitor-activity off
set -g visual-activity off

# Reload the file with Prefix r
bind r source-file ~/.tmux.conf \; display "Reloaded!"

bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

bind S \
  setw synchronize-panes \; \
  display "Sync #{?synchronize-panes,ON,OFF}"

bind R \
  setw synchronize-panes \; \
  send-keys UP\; \
  send-keys Enter\; \
  setw synchronize-panes

bind q killp

# moving between panes
set -g @vim_navigator_mapping_left "C-Left C-h"  # use C-h and C-Left
set -g @vim_navigator_mapping_right "C-Right C-l"
set -g @vim_navigator_mapping_up "C-k"
set -g @vim_navigator_mapping_down "C-j"
set -g @vim_navigator_mapping_prev ""  # removes the C-\ binding

# Pane resizing
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# set vi mode for copy mode
setw -g mode-keys vi
bind-key -T copy-mode-vi v send -X begin-selection
bind-key -T copy-mode-vi y send -X copy-pipe-and-cancel wl-copy

bind-key t display-menu \
    cal l "display-popup \"cal -n6\"" \
    top o "display-popup -E -h 100% \"btop\""

# Fuzzy find windows across ALL sessions and switch to the selected one
bind-key f split-window -v -l 30 'tmux list-windows -a -F "#S:#I: #W" | fzf --height 30 --reverse --preview "tmux capture-pane -p -t (echo {} | awk -F: '\''{print \$1\":\"\$2}'\'')" --preview-window right:50% | awk -F: "{print \$1\":\"\$2}" | xargs -I{} tmux switch-client -t {}; tmux kill-pane -t $TMUX_PANE'


#+----------------+
#+ Plugin Support +
#+----------------+
#+--- tmux-prefix-highlight ---+
set -g @prefix_highlight_fg black
set -g @prefix_highlight_bg brightcyan

#+---------+
#+ Options +
#+---------+
set -g status-interval 1
set -g status on
set -g status-right-length 150
bind-key b set-option status

#+--------+
#+ Status +
#+--------+
#+--- Layout ---+
set -g status-justify left

#+--- Colors ---+
# VHS Purple theme colors
set -g status-style bg=#0a0a0f,fg=#b8b4d4

#+-------+
#+ Panes +
#+-------+
set -g pane-border-style fg=#484858
set -g pane-active-border-style "bg=default fg=#8884d8"

#+----------------+
#+ Plugin Support +
#+----------------+
#+--- tmux-prefix-highlight ---+
set -g @prefix_highlight_output_prefix "#[fg=#0a0a0f,bg=#0a0a0f,nobold,noitalics,nounderscore]#[bg=#0a0a0f,fg=#0a0a0f]"
set -g @prefix_highlight_output_suffix ""
set -g @prefix_highlight_copy_mode_attr "fg=#78a8d8,bg=#0a0a0f,bold"
#+--------+
#+ Status +
#+--------+

#+--- Bars ---+
set -g status-left ""
set -g status-right "#{?client_prefix,#[reverse]<Prefix>#[noreverse] ,} #[fg=#16213d,bg=#0a1020,nobold,noitalics,nounderscore]#[fg=#8da8e8,bg=#16213d] #(hyprctl devices -j | jq -r '.keyboards[] | select(.main == true) | .active_keymap' | sed 's/English (US)/US/; s/Russian/RU/') #[fg=#8da8e8,bg=#16213d,nobold,noitalics,nounderscore] #[fg=#5ca673,bg=#16213d]  #(pactl get-sink-volume @DEFAULT_SINK@ | awk '{print $5}' | tr -d '%')% #[fg=#8da8e8,bg=#16213d,nobold,noitalics,nounderscore]#[fg=#4d7aa6,bg=#16213d] #(ip route | grep default | awk '{print $5}' | head -1)#[fg=#5ca673,bg=#16213d]#(nmcli connection show --active | grep -E 'vpn|tun|wireguard' | awk '{print \" | \"$1}' | head -1)#[fg=#8da8e8,bg=#16213d,nobold,noitalics,nounderscore] #[fg=#8da8e8,bg=#16213d,nobold,noitalics,nounderscore]#[fg=#4d7aa6,bg=#16213d] %Y-%m-%d #[fg=#8da8e8,bg=#16213d,nobold,noitalics,nounderscore]#[fg=#5ca673,bg=#16213d] %H:%M #[fg=#8da8e8,bg=#16213d,nobold,noitalics,nounderscore] #[fg=#5c85d9,bg=#16213d]#(~/.tmux/battery.sh)"

#+--- Windows ---+
set -g window-status-format " #[fg=#b8b4d4,bg=#12121a]#I #[fg=#b8b4d4,bg=#12121a]#W #[fg=#12121a,bg=#0a0a0f,nobold,noitalics,nounderscore] "
set -g window-status-current-format "#[fg=#0a0a0f,bg=#8884d8,nobold,noitalics,nounderscore] #[fg=#0a0a0f,bg=#8884d8]#I #[fg=#0a0a0f,bg=#8884d8]#W #[fg=#8884d8,bg=#0a0a0f,nobold,noitalics,nounderscore] "
set -g window-status-activity-style bg=#0a0a0f
set -g window-status-separator ""

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
